# Wordfolk 仕様書

## 1. コンセプト

英単語・句動詞学習アプリ。単語を孤立した暗記対象にせず、**シノニム・コロケーション・言い換えで意味的に繋がったグラフ（セマンティックネットワーク）**として学習させる。認知心理学の「拡散的活性化（spreading activation）」をUI/UXで再現し、関連語が自然に想起・再登場する体験を目指す。

アプリ名は **Wordfolk**。単語（意味ごと）が顔（identicon）を持ち、村の住人のように存在するというメタファーで、UI・コピーライティング全体を一貫させる。

---

## 2. Does / Doesn't

### Does（すること）

- **摩擦ゼロのキャプチャ**：box に単語を投げるだけ。分類を強制しない
- **見えない自動分類**：AIが意味クラスタ・接続語を判断する。ユーザーは選ばない
- **事後の低コスト訂正**：間違っていたら後で直せる。事前確認は求めない
- **視覚的アンカー**：identicon でキャラクター付けし、多義語を直感的に区別
- **能動的な深掘りの窓口**：Q&A ボタンで、興味があれば掘れる（第2段階）
- **俯瞰による偶然の発見**：グラフビューはオプトインの探索装置（第2段階）
- **ゆるい自己記録**：語彙数などをさりげなく報告。比較や評価はしない

### Doesn't（しないこと）

- 仕分け作業をユーザーに課さない（トリアージ画面は作らない）
- 成績・進捗の可視化はしない（達成率、ストリーク表示など）
- 他人との比較はしない（ランキング、フレンド機能、共有機能）
- クイズ（出題）機能は当面なし、または大幅に後回し
- 通知・リマインドはしない
- homonym（同綴異義語）関係をグラフのエッジに混ぜない（意味的に無関係なものを繋がりとして表示しない）
- 事前の完璧な分類を求めない（確認ダイアログ、承認フローを作らない）
- 重量処理（ストーリー生成等）をデフォルトにしない

---

## 3. データの考え方（概念レベル）

### 3.1 見出し語と意味クラスタの分離

単語（見出し語）と、その単語が持つ個々の意味（sense cluster）を分けて扱う。たとえば `run` という見出し語は「走る」「経営」「立候補」といった複数の意味クラスタを持ち、それぞれが類義語やコロケーションと繋がりを持つ。

### 3.2 Etymology Group（第2段階・MVP対象外）

語源が異なる同綴語（homonym、例: `bank`＝土手／銀行）を区別する考え方。

- 同じ語源から派生した意味（polysemy）は共通のコアイメージを1つ持つ
- 語源が異なる（homonym）場合は、今見ている意味のコアイメージを表示しつつ、他の語源グループへの参照リンクを添える
- この参照は意味的な繋がりではないため、**グラフ上の関連語としては扱わない**（拡散活性化の一貫性を壊さないため）

### 3.3 Q&A（第2段階・MVP対象外）

単語ごとに、AIが自動生成する「使いこなし方のストーリー」（lazy生成・1回のみ）と、ユーザーが能動的に質問して蓄積する Q&A 履歴（何度でも追加可能）を持たせる。前者は自動、後者はユーザー主導という役割分担。

---

## 4. ページ構成

### MVP対象

| ページ | 内容 |
|---|---|
| box（ホーム） | 単一inputで表現を投げ入れる。リアルタイムプレビュー、楽観的UI |
| 追加履歴 | box に投げた表現の時系列一覧。冒頭に簡易統計 |
| 単語詳細 | ul/li 木構造で接続語一覧を表示 |

### 第2段階（MVP対象外）

| ページ/機能 | 内容 |
|---|---|
| アクティビティ履歴 | 質問・閲覧などの行動ログ（追加履歴とは別ストリーム） |
| グラフ俯瞰ビュー | ズームレベルに応じて情報密度が変わる俯瞰表示（クラスタ単位→個別ノード） |
| Q&A | 単語詳細ページ内で質問→回答を蓄積 |
| ストーリー生成 | 使いこなし方・面白い話をAIが lazy 生成 |
| Etymology Group / コアイメージ | homonym分岐とコアイメージ表示 |
| 出題（クイズ） | コロケーション虫食い、言い換えチャレンジ |

---

## 5. box の入力仕様

### 5.1 パースの考え方

- 記号（`#` 等）は導入しない。**スペース区切り＋文字種判定**のみ（スマホ入力の摩擦を避けるため）
- 英字トークン → 見出し語、かな/カナ/漢字トークン → 意味のヒント
- 連続する英字トークンは結合して1つの見出し語とする（句動詞対応: `take off` のような複数語も1つの表現として扱う）
- 英字トークン・ヒントトークンいずれも数字の混在を許容する（`1st`, `1番になった人` など）。ただし数字だけのトークンはどちらにも該当せず切り捨てる
- ヒントトークンは英字の混在も許容する（`IT企業`, `Web開発者` など）。英字だけのトークンは見出し語として扱う
- 見出し語グループがヒントを挟んで分断される、ヒントが複数トークンに分かれる、といった曖昧な入力は不正入力として扱う（区切り記号を持たないため、字種だけでは意図を判別できない）
- 英語でも日本語でもないトークンは切り捨てる

例:
- `run` → 見出し語のみ
- `run 経営` → 見出し語＋ヒント
- `take off` → 句動詞として1つの見出し語
- `take off 離陸` → 見出し語＋ヒント
- `1st 3D` → 数字混じりの見出し語（`1st`, `3D` はどちらも見出し語トークン）
- `champion 1番になった人` → 見出し語＋数字混じりのヒント
- `worker IT企業` → 見出し語＋英字混じりのヒント

### 5.2 リアルタイムプレビュー

- input 直下に、パース結果をハイライト表示
  - 見出し語: 青系
  - ヒント: 緑系
  - 未確定・曖昧な部分: グレー波線（将来、句動詞境界判定を精緻化する場合）
- Todoist の Quick Add を参考に、「入力しながら解釈済み部分が見える」ことで安心感を担保する

### 5.3 楽観的UI

- Enter/送信で即座に input を空にし、次の入力に移れる
- 分類・接続語生成は完全非同期。履歴には「処理中→完了」の状態で表示

---

## 6. 分類ロジックの考え方（3段階フォールバック）

| 段階 | 条件 | 処理 |
|---|---|---|
| 1 | ヒントあり | ヒントを手がかりに既存の意味クラスタへ接続。近いものがなければ新規クラスタ作成 |
| 2 | ヒントなし | 最も一般的な意味をデフォルトとして接続 |
| 3 | 辞書・グラフ双方に未収録 | 「未分類」の孤立した項目として仮登録 |

- 誤分類は前提として許容し、事後にグラフビュー上で繋ぎ直すことで訂正コストを吸収する
- 人間の判断を仕分け作業として挟まない（ヒントという「入力時の一言」だけが唯一の人間からのシグナル）

---

## 7. 接続語生成（AI）の進め方

1. まず素のプロンプト＋構造化出力で接続語提案を生成し、「関連語がない場合は正直にそう答える」動作を検証。実際の誤答パターンを収集する
2. 収集した誤答例を教師データとして、ファインチューニングされたモデルに切り替える
3. 上記をベースに、より積極的な接続語の提案機能を追加する

段階ごとに実装を分けて残し、後から比較・切り替えができるようにしておく。

---

## 8. データ精度の担保方針

無料で使える範囲では **WordNet** のみを利用する（Wiktionary等の外部APIは運用コストの都合で見送り）。

| 情報 | 担保方法 | 精度期待値 |
|---|---|---|
| 類義語・対義語 | WordNet | 高 |
| 品詞 | WordNet | 高 |
| コロケーション | AI生成のみ | 低〜中 |
| 句動詞の意味分岐 | AI生成のみ | 低〜中 |
| 語源・homonym判定 | AI生成のみ | 低 |
| コアイメージ | AI生成のみ（解釈として明示） | 保証なし |

- WordNet はローカルデータとして保持し、外部API呼び出しに依存しない
- WordNet で裏取りできる情報には小さな出典表示を付け、それ以外はAI生成であることを隠さず明示する

---

## 9. Identicon

- 表現（見出し語、または意味クラスタ単位）から決定論的に生成される顔（アイコン）
- 同じ意味の同じ語は常に同じ顔になる
- 意味クラスタごとに顔を分けるかどうかは、Etymology Group の実装と合わせて検討する
- 配置箇所：履歴一覧の各行、単語詳細のヘッダー、（第2段階）グラフビューのノード

---

## 10. 実装順序（大枠）

1. box の入力パース（見出し語とヒントの分解、句動詞の扱い）
2. identicon（AI非依存で早期に動くものを確認できるため優先度高め）
3. 接続語生成（素のプロンプト版で誤答パターンを収集）
4. 接続語生成（収集した誤答例をもとにした改善版）
5. 接続語の提案機能の拡張

---

## 11. MVP最低限ライン

box（パース＋プレビュー＋楽観的UI）→ 分類（AI生成＋WordNet裏取り）→ identicon（見出し語単位）→ 追加履歴（シンプルな一覧＋簡易統計）→ 単語詳細（木構造のみ）

MVPで検証したい問い：**「AIが自動生成した接続語を、ユーザーは信頼して眺め続けたくなるか」**。ここが成立しなければ、Q&A・グラフビュー・Etymology Group等の拡張機能を足しても意味がないため、優先的に検証する。

---

## 12. 未確定事項

- 句動詞境界判定の精緻化レベル（MVPでは簡易的な判定にとどめる想定）
- Etymology Group 判定のタイミング（box投入時か、単語詳細を開いた時か、同一見出し語への2回目投入時か）
- Q&A回答から得られた新情報を接続語リストに自動反映するか、独立させるか
- identicon の単位を見出し語にするか意味クラスタにするか（Etymology Group実装と連動）
